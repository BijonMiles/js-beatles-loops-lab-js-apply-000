// add solution here

var empty = [];

function theBeatlesPlay(empty, instrument) {
  for (var count = 0; count<=empty.length; count++){
    
    return(empty[count] + " plays " + instrument[count])
  }
}

theBeatlesPlay(musicians, instrument)

var array = [];

function johnLennonFacts(array){
  var num = 0;
  while(num<array.length){
    return(array[num] + "! ")
    num++;
  }
}

johnLennonFacts("I love the Beatles")
